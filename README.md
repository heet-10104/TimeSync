# TimeSync


## Installation
First install [Go](http://golang.org).

Then run:
```bash
go install github.com/azryx/timesync@latest
```

## Usage 
Run the server using:
```bash
timesync --port <port>
```
